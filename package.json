{
  "name": "cognition-take-home-demo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w starter",
    "build": "npm run build -w starter",
    "lint": "npm run lint -w starter",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed"
  },
  "prisma": {
    "seed": "node packages/framework/prisma/seed.mjs"
  },
  "devDependencies": {
    "dotenv": "^18.0.3",
    "prisma": "6.12.0"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  }
}

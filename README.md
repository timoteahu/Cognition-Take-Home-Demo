# Cognition Take Home Demo

Bare-bones starter app.

## Run

```bash
pip install -r requirements.txt
uvicorn app:app --reload
```

Then hit it:

```bash
curl localhost:8000
# {"message": "Hello, world!"}
```
